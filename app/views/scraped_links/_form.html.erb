<%= form_with(model: scraped_link) do |form| %>
  <% if scraped_link.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scraped_link.errors.count, "error") %> prohibited this scraped_link from being saved:</h2>
      <ul>
        <% scraped_link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
